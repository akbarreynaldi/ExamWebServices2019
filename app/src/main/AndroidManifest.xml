<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unsil.if16.volunteer">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.unsil.if16.volunteer.MainActivity">
        </activity>
        <activity android:name="com.unsil.if16.volunteer.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.unsil.if16.volunteer.DaftarActivity">
        </activity>
        <activity android:name="com.unsil.if16.volunteer.EventListActivity">
        </activity>
        <activity android:name="com.unsil.if16.volunteer.EventDetailActivity"
                  android:parentActivityName="com.unsil.if16.volunteer.EventListActivity">
        </activity>
    </application>

</manifest>